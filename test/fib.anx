pub printi(i: i32);

pub main(): i32 {
    printi(fib(20));
    ret 0;
}

fn fib(n: i32): i32 {
    if !n ret 0;

    var ppn: i32;
    var pn = 0;
    var cn = 1;

    while n > 1 : n = n - 1 {
        ppn = pn;
        pn = cn;
        cn = ppn + pn;
    }

    ret cn;
}
