pub fn printi(i: i32);

fn main() {
    printi(fib(20));
}

fn fib(n: i32): i32 {
    if !n ret 0;

    var ppn: i32, pn = 0, cn = 1;

    while n > 1 : n = n - 1 {
        ppn = pn;
        pn = cn;
        cn = ppn + pn;
    }

    ret cn;
}
